# Demo audio

Place legally licensed MP3 files here using the paths in `storage/songs.json`, for example `hindi/neon-raag.mp3`. The player loads `audioPath` through the backend. No copyrighted audio is included in this source repository.
