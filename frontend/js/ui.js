const circularDemo={nodes:[],current:0,nextId:1};
const UI={format(seconds=0){return `${Math.floor(seconds/60)}:${String(Math.floor(seconds%60)).padStart(2,'0')}`},card(song){const liked=state.favorites.has(song.id);return `<article class="song-card"><img src="${song.coverPath}" alt="${song.title} cover" loading="lazy"><h3>${song.title}</h3><p>${song.artist}</p><div class="meta">${song.language} · ${song.genre}</div><div class="card-actions"><button class="favorite ${liked?'liked':''}" data-id="${song.id}" aria-label="Favorite">${liked?'♥':'♡'}</button><button class="play-card" data-id="${song.id}" aria-label="Play ${song.title}">▶</button></div></article>`},render(songs,title){document.querySelector('#section-title').textContent=title;document.querySelector('#song-grid').innerHTML=songs.length?songs.map(this.card).join(''):`<div class="empty">No songs found.<br>Try a song, artist, language, or genre.</div>`},async circular(){try{const d=await API.debug();document.querySelector('#node-info').textContent=`HEAD: ${d.head} · CURRENT: ${d.current} · TAIL: ${d.tail} · SIZE: ${d.size}`;if(!circularDemo.nodes.length){circularDemo.nodes=state.songs.slice();circularDemo.nextId=state.songs.length+1;this.addCircularControls()}this.renderCircularDemo()}catch(e){}},addCircularControls(){const panel=document.querySelector('#circular-panel');if(!panel||document.querySelector('#circular-controls'))return;const controls=document.createElement('div');controls.id='circular-controls';controls.style='display:flex;flex-wrap:wrap;gap:8px;margin:12px 0';controls.innerHTML='<button data-circular-action="previous">Prev</button><button data-circular-action="next">Next</button><button data-circular-action="insert">Insert demo node</button><button data-circular-action="delete">Delete current</button><button data-circular-action="reset">Reset</button>';panel.insertBefore(controls,document.querySelector('#node-flow'));const note=document.createElement('p');note.id='circular-explanation';note.style='color:#625c69;margin:5px 0 14px';note.textContent='The tail points back to the head, so traversal never reaches null.';panel.insertBefore(note,document.querySelector('#node-flow'))},renderCircularDemo(){const nodes=circularDemo.nodes;if(!nodes.length)return;const current=Math.min(circularDemo.current,nodes.length-1);circularDemo.current=current;document.querySelector('#node-flow').innerHTML=nodes.map((song,index)=>`<div class="node ${index===current?'current':''}"><small>${index===0?'HEAD ':''}${index===current?'CURRENT ':''}${index===nodes.length-1?'TAIL':''}</small><strong>${song.title}</strong><span>${index===nodes.length-1?'next → HEAD':'next → '+nodes[(index+1)%nodes.length].title}</span></div>`).join('');},circularAction(action){const nodes=circularDemo.nodes;if(action==='next'){circularDemo.current=(circularDemo.current+1)%nodes.length;document.querySelector('#circular-explanation').textContent='CURRENT moved to next. At TAIL, next wraps around to HEAD.'}if(action==='previous'){circularDemo.current=(circularDemo.current-1+nodes.length)%nodes.length;document.querySelector('#circular-explanation').textContent='CURRENT moved to the previous node by scanning for its predecessor.'}if(action==='insert'){const id=circularDemo.nextId++;nodes.splice(circularDemo.current+1,0,{title:`Demo Node ${id}`});circularDemo.current=(circularDemo.current+1)%nodes.length;document.querySelector('#circular-explanation').textContent='Inserted a node after CURRENT and linked it into the circle.'}if(action==='delete'&&nodes.length>1){const removed=nodes.splice(circularDemo.current,1)[0];circularDemo.current%=nodes.length;document.querySelector('#circular-explanation').textContent=`Deleted ${removed.title}; the previous node now points to the next node.`}if(action==='reset'){circularDemo.nodes=state.songs.slice();circularDemo.current=0;document.querySelector('#circular-explanation').textContent='The tail points back to the head, so traversal never reaches null.'}this.renderCircularDemo()}};
document.addEventListener('click',event=>{const action=event.target.dataset.circularAction;if(action)UI.circularAction(action)});
